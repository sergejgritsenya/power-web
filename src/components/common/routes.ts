export const news_routes = {
  root: "/news",
  get: (id: string) => `/news/${id}`,
}

export const shop_routes = {
  root: "/shop",
  get: (id: string) => `/shop/${id}`,
}

export const tournament_routes = {
  root: "/tournament",
  get: (id: string) => `/tournament/${id}`,
}
